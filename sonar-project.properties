sonar.projectKey=alce65_week8.users
sonar.organization=alce65

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=week8.users
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

sonar.test.inclusions=./src/**/*.test.*

sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.coverage.exclusions=src/index.ts, src/app.ts, src/reportWebVitals.ts, src/**/*.test.*, src/**/e2e, src/client, src/routers, src/entities, src/repositories/**/*.model.ts'
        

#sonar.scm.provider=git

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
